class Solution {
    public String convert(String s, int numRows) {
        //先考虑如果一行就直接输出
        if(numRows==1){
            return s;
        }
        
        String result[3][s.length()];
        for(int i = 0;i<s.length();i++){
            
        }
    }